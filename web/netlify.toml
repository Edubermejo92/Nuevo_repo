# PadelPulse Live — version web (movil, tablet y ordenador).
# Es el MISMO code.html que va dentro de la app Android: para actualizar el
# sitio basta con volver a ejecutar tools/sync-web.sh y desplegar esta carpeta.
[build]
  publish = "."
  command = ""

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
